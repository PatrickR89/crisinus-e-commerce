import React from "react";
import { PageHero } from "../components";

const OrderPage = () => {
  return (
    <main>
      <PageHero title="order" />
      <h2>order page</h2>
    </main>
  );
};

export default OrderPage;
